# Simple Android app that uses Rust

This app is written in Kotlin.

Build the app:
```bash
$ ./gradlew build
```

Install to connected device:
```bash
$ ./gradlew uninstallDebug && ./gradlew installDebug
```
